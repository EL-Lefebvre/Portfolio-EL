import React from 'react'
import styled from "styled-components";
const ProjectCard = ({title, link}: any) => {
    return (
        <div>
            
        </div>
    )
}

export default ProjectCard
